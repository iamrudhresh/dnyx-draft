# Claude Project Instructions

- Project uses pnpm workspaces and Turborepo.
- Web app lives in `apps/web/`.
- Modular packages live in `packages/`.
- Format and lint with `pnpm check`.
- Run tests with `pnpm test`.
